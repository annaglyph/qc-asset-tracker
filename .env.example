# -----------------------------------------------------------------------------
# QC Asset Crawler – Example Environment Configuration
# Copy this file to ".env" and fill in the appropriate values for your environment.
# -----------------------------------------------------------------------------

# ---------------------------------------------------------------------
# TRAK Integration
# ---------------------------------------------------------------------
TRAK_BASE_URL=https://qa-trak.eikongroup.io/api/v1/
TRAK_ASSET_TRACKER_API_KEY=!replace-with-your-api-key

# ---------------------------------------------------------------------
# QC System Settings
# ---------------------------------------------------------------------
# Extended attribute key (used for tagging files with QC status)
QC_XATTR_KEY=user.eikon.qc

# Hash cache filename (per-directory caching of hashes)
QC_HASHCACHE_NAME=.qc.hashcache.json

# Sidecar naming (for file and sequence modes)
QC_SIDE_SUFFIX_FILE=.qc.json
QC_SIDE_NAME_SEQUENCE=qc.sequence.json

# ---------------------------------------------------------------------
# Logging / Debug
# ---------------------------------------------------------------------
# LOG_LEVEL can be: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
